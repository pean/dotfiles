name: hemnet-dev
root: ~/hemnet/src/hemnet-ng

pre: open /Applications/Docker.app/ && docker network create hemnet-stack && hemnet stack start && make update && make services

windows:
  - editor:
      layout: f552,204x51,0,0{80x51,0,0[80x15,0,0,0,80x17,0,16,2,80x17,0,34,3],123x51,81,0,1}
      panes:
        - bin/rails console
        - git diff
        - git status
        - nvim .
  - server: make server
  - db: bin/rails dbconsole
