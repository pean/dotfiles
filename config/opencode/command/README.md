# OpenCode Commands

This directory contains custom slash commands for OpenCode.

Add your custom command files here (`.md` files) to make them available globally in OpenCode.

These commands are managed via RCM and will be symlinked to `~/.config/opencode/command/`
