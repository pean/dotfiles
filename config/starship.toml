format = """$username$hostname$shlvl$singularity$kubernetes${custom.git_repo}${custom.worktree}$directory$vcsh$fossil_branch$fossil_metrics$git_branch$git_commit$git_state$git_metrics$git_status$hg_branch$hg_state$pijul_channel$docker_context$package$bun$buf$c$cmake$cobol$cpp$crystal$daml$dart$deno$dotnet$elixir$elm$erlang$fennel$fortran$gleam$golang$gradle$guix_shell$haskell$haxe$helm$java$julia$kotlin$lua$meson$mojo$nim$nodejs$ocaml$odin$opa$perl$php$pixi$pulumi$purescript$python$quarto$raku$red$rlang$ruby$rust$scala$solidity$swift$terraform$typst$vagrant$vlang$xmake$zig$nix_shell$conda$mamba$spack$memory_usage$aws$gcloud$openstack$azure$nats$direnv$env_var$mise$sudo$line_break$jobs$battery$status$os$container$netns$shell$character"""

# right_format = """$cmd_duration $time"""

[time]
disabled = true
format = "[$time]($style)"
time_format = "%-m-%-d%k%M"

[git_branch]
symbol = " "
format = "[$symbol$branch]($style) "

# [git_status]
# stashed = ""
# format = '([$all_status $ahead_behind]($style))'

# [git_metrics]
# disabled = true

[directory]
truncate_to_repo = true
truncation_length = 4
style = "cyan"
# Show only the subpath within the repo (repo name shown by custom.git_repo).
# Outside git repos, falls back to normal directory display.
repo_root_format = "([$path$read_only]($style) )"
repo_root_style = "cyan"
before_repo_root_style = "dimmed cyan"
format = "[$path]($style)[$read_only]($read_only_style) "

[custom.git_repo]
description = "Show git repo name instead of directory path"
command = '''
  common=$(git rev-parse --git-common-dir 2>/dev/null) || exit 1
  case "$common" in
    /*) name=$(basename "$common" .git) ;;
    *)  name=$(basename "$(git rev-parse --show-toplevel 2>/dev/null)") ;;
  esac
  printf "%s" "$name"
'''
when = "git rev-parse --git-dir > /dev/null 2>&1"
format = "([$output]($style) )"
style = "bold blue"
shell = ["sh"]

[custom.worktree]
description = "Show indicator when inside a git worktree"
command = '''
  git_dir=$(git rev-parse --absolute-git-dir 2>/dev/null) || exit 1
  common=$(git rev-parse --git-common-dir 2>/dev/null) || exit 1
  case "$common" in
    /*) ;;
    *) common="$(cd "$common" 2>/dev/null && pwd)" ;;
  esac
  [ "$git_dir" != "$common" ] && printf "" || exit 1
'''
when = "git rev-parse --git-dir > /dev/null 2>&1"
format = "([$output]($style) )"
style = "yellow"
shell = ["sh"]

# [aws]
# disabled = true
#
# [shell]
# disabled = true

[cmd_duration]
format = "[$duration]($style)"
min_time = 0
show_milliseconds = true
show_notifications = false

[docker_context]
disabled = true

[package]
disabled = true

# preset 
[bun]
format = "[$symbol]($style)"

[buf]
format = "with [$symbol]($style)"

[c]
format = "[$symbol($name)]($style)"

[cmake]
format = "[$symbol]($style)"

[cobol]
format = "[$symbol]($style)"

[cpp]
format = "[$symbol($name)]($style)"

[crystal]
format = "[$symbol]($style)"

[daml]
format = "[$symbol]($style)"

[dart]
format = "[$symbol]($style)"

[deno]
format = "[$symbol]($style)"

[dotnet]
format = "[$symbol(🎯 $tfm )]($style)"

[elixir]
format = '[$symbol]($style)'

[elm]
format = '[$symbol]($style)'

[erlang]
format = '[$symbol]($style)'

[fennel]
format = '[$symbol]($style)'

[fortran]
format = '[$symbol]($style)'

[gleam]
format = '[$symbol]($style)'

[golang]
format = '[$symbol]($style)'

[gradle]
format = '[$symbol]($style)'

[haskell]
format = '[$symbol]($style)'

[haxe]
format = '[$symbol]($style)'

[helm]
format = '[$symbol]($style)'

[java]
format = '[$symbol]($style)'

[julia]
format = '[$symbol]($style)'

[kotlin]
format = '[$symbol]($style)'

[lua]
format = '[$symbol]($style)'

[meson]
format = '[$symbol]($style)'

[mojo]
format = 'with [$symbol]($style)'

[nim]
format = '[$symbol]($style)'

[nodejs]
format = '[$symbol]($style)'

[ocaml]
format = '[$symbol(\($switch_indicator$switch_name\) )]($style)'

[odin]
format = '[$symbol]($style)'

[opa]
format = '[$symbol]($style)'

[perl]
format = '[$symbol]($style)'

[php]
format = '[$symbol]($style)'

[pixi]
format = '[$symbol($environment )]($style)'

[pulumi]
format = '[$symbol$stack]($style)'

[purescript]
format = '[$symbol]($style)'

[python]
format = '[$symbol]($style)'

[quarto]
format = '[$symbol]($style)'

[raku]
format = '[$symbol]($style)'

[red]
format = '[$symbol]($style)'

[rlang]
format = '[$symbol]($style)'

[ruby]
format = '[$symbol]($style)'

[rust]
format = '[$symbol]($style)'

[scala]
format = '[$symbol]($style)'

[solidity]
format = '[$symbol]($style)'

[swift]
format = '[$symbol]($style)'

[typst]
format = '[$symbol]($style)'

[vagrant]
format = '[$symbol]($style)'

[vlang]
format = '[$symbol]($style)'

[xmake]
format = "[$symbol]($style)"

[zig]
format = '[$symbol]($style)'
